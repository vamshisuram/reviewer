//#region src/stream/filter.ts
/**
* @deprecated use `stream.filter` instead. This will be removed in the next minor version.
*/
function filter(stream, cond) {
	return stream.pipeThrough(new TransformStream({ transform(event, controller) {
		if (cond(event)) controller.enqueue(event);
	} }));
}

//#endregion
export { filter };
//# sourceMappingURL=filter.js.map